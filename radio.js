console.log('my fish is 10cm long')

const fish = (first, second) => {
    console.log(`The first is: ${first} and the second is: ${second}`)
    return first*second;
}

this.output = fish(15, 20)

class NewKlass{

    ayy = () => {

    }

    bay = (a, b, c) => {
        console.log(`the three input is: ${a}, ${b}, ${c}`)
    }
}

let collect1 = new NewKlass()//

collect1.bay()